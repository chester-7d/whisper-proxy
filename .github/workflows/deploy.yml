name: Trigger Vercel Deployment

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: |
          curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_XRoewi90ohEtjVUFrWGvYfdwVFV5/MadmCRGPlh"
